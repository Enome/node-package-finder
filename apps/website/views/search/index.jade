extends ../layout

block content


  .container

    .row
      .span12
        .search-info Searched for <strong>#{q}</strong> and found <strong>#{ result.total_rows } packages</strong>

    .row
      for package in result.rows
        .span6
          .package-small
            h3
              a= package.doc.name

            p= package.doc.description
